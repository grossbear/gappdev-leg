/////////////////////////////////////////////////////////////////////////////////////////////
// WinMain.cpp
/////////////////////////////////////////////////////////////////////////////////////////////

#include <windows.h>
#include "ApplWindow.h"
#include "../Application.h"

extern CApplication *CreateApplication(LPTSTR lpCmdLine);

#define WINMAIN _tWinMain

/////////////////////////////////////////////////////////////////////////////////////////////
int WINAPI WINMAIN(HINSTANCE hInstance,HINSTANCE hPrevInstance,LPTSTR lpCmdLine, int nShowCmd)
{
	CApplication *app = CreateApplication(lpCmdLine);
	CApplWindow *applwnd = new CApplWindow(hInstance);
	app->Run(applwnd);

	delete (applwnd);
	delete (app);

	return 0;
}

/////////////////////////////////////////////////////////////////////////////////////////////